generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url = "***"
}

model View {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  slug      String
}